# HMAC secret for shared folder request signing
# Generate a random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WHISPER_HMAC_SECRET=change-me-to-a-random-32-byte-hex-string
